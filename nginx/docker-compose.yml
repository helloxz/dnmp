version: '3'
services:
  xcdn:
    image: helloz/xcdn:alpine-1.24.0
    #environment:
      
    network_mode: "host"
    volumes:
      - ../wwwroot:/var/www/html
      - ./data:/data/xcdn
      - ./logs:/data/logs
      - ./conf/nginx.conf:/usr/local/nginx/conf/nginx.conf
      
    restart:
      always